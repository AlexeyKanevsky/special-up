fatal: The current branch 'catalog-filter' has no upstream branch.
To push the current branch and set the remote as upstream, use

    git push --set-upstream origin 'catalog-filter'

To have this happen automatically for branches without a tracking
upstream, see 'push.autoSetupRemote' in 'git help config'.
fatal: You must specify a repository to clone.
фатальный: текущая ветка «каталог-фильтр» не имеет восходящей ветки.
Чтобы нажать текущую ветку и установить удаленный как восходящий, используйте

     git push --set-upstream origin 'каталог-фильтр'

Чтобы это происходило автоматически для веток без отслеживания
вверх по течению, см. «push.autoSetupRemote» в «конфигурации справки git».
фатальный: вы должны указать репозиторий для клонирования.

вот же жопа!